import Contact from '../components/sections/Contact';

export default function ContactPage() {
  return (
    <div style={{ background: '#0a0a0a', minHeight: '100vh', paddingTop: 'clamp(60px, 8vw, 80px)' }}>
      <Contact />
    </div>
  );
}
